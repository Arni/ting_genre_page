<?php

/**
 * @file
 * content_type plugin generated by drush ctools plugin stub.
 */

$plugin = array(
  'title' => t('TITLE'),
  'description' => t('DESCRIPTION.'),
  'content_types' => array('related_links'),
  'defaults' => array('ting_genre_page_related_links_YOUR_VAR' => ''),
  'category' => t('CATEGORY'),
  'single' => TRUE,
);

/**
 * 'Edit' callback for the content type.
 */
function ting_genre_page_related_links_content_type_edit_form($form, &$form_state) {
  $conf = $form_state['conf'];
  $form['ting_genre_page_related_links_YOUR_VAR'] = array(
    '#type' => 'textfield',
    '#title' => t('TITLE'),
    '#description' => t('DESCRIPTION.'),
    '#default_value' => !empty($conf['ting_genre_page_related_links_YOUR_VAR']) ? $conf['ting_genre_page_related_links_YOUR_VAR'] : '',
  );
  return $form;
}

/**
 * The submit form stores the data in $conf.
 */
function ting_genre_page_related_links_content_type_edit_form_submit($form, &$form_state) {
  foreach (array_keys($form_state['plugin']['defaults']) as $key) {
    if (isset($form_state['values'][$key])) {
      $form_state['conf'][$key] = $form_state['values'][$key];
    }
  }
}

/**
 * Put the output for your content type in the pane object's content.
 */
function ting_genre_page_related_links_content_type_render($subtype, $conf, $args, $context) {
  $pane = new stdClass();
  $pane->content = '';
  return $pane;
}
